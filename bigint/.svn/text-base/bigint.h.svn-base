//Curtiss Wiggins
//bigint.h
//cs33001
#ifndef CS33001_BIGINT_H_
#define CS33001_BIGINT_H_
#include<iostream>
	const int MAX_SIZE = 200;

class bigint
{
public:
    bigint();//used to initialize bigint to 0.
    bigint(char num[]);//used to initiailize bigint to a provide char
    bigint(int);//initialize bigint to an int provide
    bool operator ==(bigint);//checks to see if two bigints are the same.
	bool operator==(int);//checks to see of bigint equals a provide int.
    void output(std::ostream& out);//prints bigints
private:                       

    int digit[MAX_SIZE];    
};
#endif
